#include "main.h"

/**
 *  - check the code.
 *  - main - entry point
 *  - returns 1 if c is lowercase
 *  - returns 0 if c is lowercase
 *  - returns 0 if success
 */
int main(void)
{
	char c;
	{
	if (c < '96' && c > '122')
	return 1
    } else {
        return 0;
    }
}	putchar ('\n')
	
		
